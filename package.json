{
  "name": "hardhat-airsign-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Deploy smart contracts from your dev machine. Sign transactions and run tasks from anywhere — no private keys needed.",
  "repository": {
    "type": "git",
    "url": "https://github.com/harshitbwc/hardhat-airsign.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "npm install && npm run build:plugin && cd example && npm install",
    "build": "npm run build --workspaces",
    "build:plugin": "npm run build -w packages/plugin",
    "build:app": "rm -rf packages/app/dist && npm run build -w packages/app",
    "rebuild": "npm run build:plugin && npm run build:app",
    "test": "npm run test -w packages/plugin",
    "dev:app": "npm run dev -w packages/app",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
